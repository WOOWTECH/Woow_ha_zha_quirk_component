[pytest]
# tests/ha needs pytest-asyncio in auto mode: every Home Assistant fixture (`hass` above
# all) is an async generator, and without this they are handed to the test un-awaited.
# tests/standalone does not use pytest-asyncio and simply warns that the option is unknown.
asyncio_mode = auto
